@model IEnumerable<HoliDayRental.Models.BienOptions.BienForMembreList>

@{
    ViewData["Title"] = "VisualizzaTuttiBeniONEmembre";
}

<div style="margin-left:20%">
    <h1>VisualizzaTuttiBeniONEmembre</h1>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.titre)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DescCourte)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DateCreation)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.titre)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DescCourte)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DateCreation)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                        @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                    </td>
                </tr>
            }
        </tbody>
    </table>
    @Model.Select(m=>m.)
    @Html.Partial("_membreLabel", Model.Select(m=> { 

}))
</div>
